export const createOrganization = {
    workSpaceCodeAlreadyExist: 'Workspace code is already in use',
     invalidWorkspaceCode :'Workspace code may only contain lowercase letters, numbers, and hyphens'
} as const;